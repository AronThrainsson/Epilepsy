import { View, Text } from 'react-native';

export default function WatchScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Smartwatch Connect Screen</Text>
    </View>
  );
}